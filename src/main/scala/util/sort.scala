package main.scala.util

object higherSort {
  /**
   * @param ops a list of comparators that can sort the given list by primary, secondary, etc.
   */
  def higherSort[T](l: List[T], ops: List[(T, T) => Boolean]) = ???
  
}